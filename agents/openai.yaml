interface:
  display_name: "AI Native Loop"
  short_description: "Diagnose and rebuild AI-native workflow loops"
  default_prompt: "Use $ai-native-loop to diagnose the current loop, build a task packet, read feedback, and produce a better next input."

policy:
  allow_implicit_invocation: true
