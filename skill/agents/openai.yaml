interface:
  display_name: "AI Native Loop"
  short_description: "Turn work into an ai-native iteration loop"
  default_prompt: "Use $ai-native-loop to restructure this task into a clearer input, execution, feedback, and re-input loop."

policy:
  allow_implicit_invocation: true
